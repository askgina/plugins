# Public eval results handoff

Implementation, repository gates, clean-install artifact verification and synthetic CLI/browser proof are complete. Measured-evidence acceptance remains blocked.

This continues the `review-wayfinder-maps` checkpoint at `ace7e54` in `askgina/plugins`. The documentation step returned "Output accepted" and is complete. Do not restart or inspect the finished autodoc workflow.

## Start here

- [Sid's consumer handoff and verification record](HANDOFF.md)
- [Accepted decisions](planning/DECISIONS.md)
- [Public field reference](planning/PUBLIC_CONTRACT.md) and [canonical synthetic fixtures](planning/fixtures/)
- [Exporter request and configuration reference](planning/EXPORTER_REFERENCE.md)
- [Map and delivery tickets](planning/MAP.md)

The schemas, fixtures and field reference are repository-backed consumable artifacts. Temporary directories hold only disposable proof inputs, copies and receipts. The portable checkpoint superseded temporary-only planning, not the restrictions on GitHub issues/PRs, deployment, production changes, live MCP calls, paid evaluations or financial execution.

## Completed implementation

- Strict v1 contracts, canonical tuple-derived attempt identities, bounded conformance metrics and explicit unavailable/unranked states.
- Opt-in capture with unchanged default aggregate bytes, exact saved-report binding, exclusive mode-0600 output and fatal UTF-8/BOM handling.
- Aggregate adapter with the restored zero-observation/nonzero-token-total rejection. Schema-validated digest fields no longer fail address-text heuristics; ordinary public text remains checked.
- Required configuration component identities and matching run settings, without claiming provider attestation.
- Recorded manual review, immutable corrections, case-folded history protection and privacy withdrawal that removes old result and notice bytes. Revision counters are safe integers with contiguous history, not an arbitrary 50-revision cap.
- Browser file selection at `/#/handoff`, original-byte SHA-256 pairing, standalone/index-only disclosures, historical warnings, withdrawn/tampered result hiding, selection replacement and mobile navigation containment. Runtime package support remains unchanged.

## Verification and remaining acceptance

The parent built contracts before real package/app TypeScript checks. The first compiler run exposed 45 errors; those were repaired without suppressions. Focused contracts/evals/app regressions passed, and the compiled synthetic pipeline exercised capture, aggregate/detail/withheld/incomplete exports, correction, withdrawal, later notice replacement and rejection paths. The actual Chromium consumer displayed those exports and rejected altered bytes and malformed text.

Exact commands, outcomes, receipt references and final gate status belong in [HANDOFF.md](HANDOFF.md). Build/typecheck alone is not package acceptance. Run every command in `ai_docs/contributing.md`, including `bun run check`, `bun run artifacts` and `bun run verify:artifacts`. Frozen installation is required; regenerate the lock only for an intentional dependency change. Keep tests in discovered `__tests__` directories and derive the artifact allowlist from actual `vp pack` output.

The temporary `proof/*.mjs` helpers were used and removed from the repository after the synthetic proof. Their executed copies and receipts remain outside the checkout; the nine canonical JSON fixtures remain here.

No authorized saved measured aggregate report or genuine retained new-run case breakdown was supplied. Synthetic replay and screenshots do not close either criterion. Do not run a live evaluation to manufacture acceptance. Once authorized existing evidence and its recorded manual review are supplied, export and display both through the same verified path.
