import { PageShell } from "../../components/eval-ui";

export function PrototypeMethodologyPage() {
  return (
    <PageShell active="canary">
      <div className="eval-container eval-hero">
        <h1 className="eval-title">
          Methodology<span className="eval-dot">.</span>
        </h1>
        <p className="eval-description">Prototype stub — implementation pending.</p>
      </div>
    </PageShell>
  );
}
